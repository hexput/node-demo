{
  "name": "hexput-demo",
  "version": "1.0.0",
  "description": "A demo app for hexput lang",
  "license": "GPL-2.0",
  "author": "ErdemGKSL",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start": "pnpm run --recursive --if-present start",
    "dev": "pnpm run --recursive --if-present dev",
    "build": "pnpm run --recursive --if-present build"
  },
  "dependencies": {
    "dotenv": "^16.4.7",
    "ts-patch": "^3.3.0",
    "typescript": "^5.7.3",
    "typescript-transform-paths": "^3.5.3"
  },
  "devDependencies": {
    "@types/node": "^22.13.5"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild"
    ]
  }
}